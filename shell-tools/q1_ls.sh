#!/bin/bash

# -a            Includes all files, including hidden files
# -lh           Sizes are listed in human readable format
# -t            Files are ordered by recency
# --color=auto  Output is colorized

/bin/ls -alht --color=auto
